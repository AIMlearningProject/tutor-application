<!DOCTYPE html>
<html lang="<%= currentLocale %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= __('errors.generalError') %> - <%= __('app.title') %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <div class="header-container">
            <div class="logo-section">
                <img src="/Copsa-logo.jpg" alt="COPSA Logo" class="logo">
                <h1 class="app-title"><%= __('app.title') %></h1>
            </div>
            <div class="nav-section">
                <div class="language-switcher">
                    <a href="/language/en" class="lang-btn <%= currentLocale === 'en' ? 'active' : '' %>">EN</a>
                    <a href="/language/fi" class="lang-btn <%= currentLocale === 'fi' ? 'active' : '' %>">FI</a>
                </div>
                <% if (user) { %>
                    <a href="/auth/logout" class="btn btn-secondary btn-small"><%= __('nav.logout') %></a>
                <% } else { %>
                    <a href="/auth/google" class="btn btn-primary btn-small"><%= __('nav.login') %></a>
                <% } %>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="card text-center">
            <h2 class="card-title"><%= __('messages.error') %></h2>
            <p><%= message || __('errors.generalError') %></p>
            <div class="mt-3">
                <a href="/" class="btn btn-primary"><%= __('home.welcome') %></a>
            </div>
        </div>
    </div>

    <footer>
        <p><%= __('footer.poweredBy') %></p>
    </footer>
</body>
</html>
